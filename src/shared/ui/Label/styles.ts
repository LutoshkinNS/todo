import styled from "styled-components";

export const Label = styled.label`
  font-size: 16px;
  line-height: 24px;
  cursor: pointer;
  color: inherit;
  overflow: hidden;
  text-overflow: ellipsis;
  user-select: none;
`;
